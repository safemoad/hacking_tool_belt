if .htb
-add /etc/hosts like: echo "(ip_address) (domain)" | sudo tee -a /etc/hosts

-subdomains?

if smb
-L

Others:

start reverse shell
  nc -lvnp 9001
start webserver
  python3 -m http.server 8000
