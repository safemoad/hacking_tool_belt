if .htb
-add /etc/hosts like: echo "(ip_address) (domain)" | sudo tee -a /etc/hosts

-subdomains?

if smb
-L

Others:

start reverse shell
  nc -lvnp 9001
start webserver
  python3 -m http.server 8000

Need to build core instruction set/markdown

Ideas for AI skills:
-nmap create directory and all the options, check for redirect and add to hosts
-wordlists
-starting reverse shells
-starting webserver
-webserver enumeration (fuff)
-smb enumeration
-decrypting stuff

MoE thoughts:
-enumeration agent, fast and cheap
-documentation agent, slow and cheap
-PoC/Research agent, slow and expensive
-Privesc agent, slow and expensive
